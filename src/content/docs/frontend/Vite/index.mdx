---
title: Overview
sidebar:
  order: 2
---

import {LinkCard,CardGrid} from '@astrojs/starlight/components'
import { getCollection, getEntry } from 'astro:content';
export const tauriArticles = (await getCollection('docs')).filter(val=>val.slug.includes('frontend/vite')).filter(val=>!val.slug.endsWith('/vite'));

<CardGrid>
{ tauriArticles.map(article=>(
  <LinkCard title={article.data.title} description={article.data.description} href={'/tauri-by-simon/'+article.slug} />
)) }
</CardGrid>
