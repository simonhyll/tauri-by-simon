---
title: Overview
sidebar:
  order: 1
  badge:
    text: Planned
    variant: caution
---

import {LinkCard,CardGrid} from '@astrojs/starlight/components'
import { getCollection, getEntry } from 'astro:content';
export const tauriArticles = (await getCollection('docs')).filter(val=>val.slug.includes('concepts/web')).filter(val=>!val.slug.endsWith('/web'));

<CardGrid>
{ tauriArticles.map(article=>(
  <LinkCard title={article.data.title} description={article.data.description} href={'/tauri-by-simon/'+article.slug} />
)) }
</CardGrid>

