---
title: Tauri by Simon
template: splash
hero:
  tagline: My unofficial opinionated articles on using Tauri
  image:
    file: ../../assets/icon.png
  actions:
    - text: Start reading
      link: /tauri-by-simon/getting_started/
      icon: right-arrow
      variant: primary
    - text: Read the official docs
      link: https://beta.tauri.app
      icon: external
---

import HeroBackground from "../../components/HeroBackground.astro";

<HeroBackground />

## Articles

import {Card,CardGrid} from '@astrojs/starlight/components'
import { getCollection, getEntry } from 'astro:content';
export const articles = (await getCollection('docs')).filter(val=>val.data.title !== 'Overview' && val.slug !== 'index').filter(val=>val.data.sidebar.badge?.text !== 'Planned' && val.data.sidebar.badge?.text !== 'WIP');

{console.log(articles)}

<CardGrid>
{ articles.map(article=>(
  <Card title={article.data.title} icon="document">
  {article.data.description}
  <br/><a href={'/tauri-by-simon/'+article.slug}>Read article</a>
  </Card>
)) }
</CardGrid>
